const height = 10;
const base = 4;

const perimeter = 2 * (height + base);
const area = height * base;

const text1 = 'The perimeter of a rectangle with the sides of  ';
const text2 = 'The area of a rectangle with the sides of  ';

console.log(text1 + height + ' and ' + base + ' = ' + perimeter);
console.log(text2 + height + ' and ' + base + ' = ' + area);