const firstNumber = 1;
const secondNumber = 2;
if (firstNumber === secondNumber) {
  console.log('They are the same number');
} else if (firstNumber > secondNumber) {
    const temp = `${firstNumber} is bigger than ${secondNumber}`;
  console.log(temp);
} else if (firstNumber < secondNumber) {
    const temp1 = `${secondNumber} is bigger than ${firstNumber}`;
    console.log(temp1);
}

