let firstNumber = 5;
let secondNumber = 10;

if (firstNumber > secondNumber){
    let temp = `Number ${firstNumber} is bigger than ${secondNumber}`
    console.log(temp);
}else{
    let temp2 = `Number ${firstNumber} is smaller than ${secondNumber}`
    console.log(temp2);
}
